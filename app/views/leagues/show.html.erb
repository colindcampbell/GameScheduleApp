
<% if session[:remember_token] && session[:remember_token] == @league.user_id %>
	<div class="container-fluid">

		<div class="row">
		
			<div class="col-md-3">
				
					<span class="btn btn-default btn-block" data-toggle="collapse" data-target="#myLeagues">
						<p id="myTitle" name="scheduleTitle">( Leagues )</p>
					</span>
				
				<div id="myLeagues" class="collapse out"><br>
				
					<% League.where(:user_id => current_user.id).each do |l| %>
					
						<div class="btn-group-vertical btn-group-justified">
							<%= link_to l.name, league_path(l.id), class: 'btn btn-default btn-block', id: 'myTitle' %>
						</div>
							<div class="list-group-item">
									<strong class="list-group-item-text">This league runs from <%= l.start_date.strftime("%A, %b %d") %> through <%= l.end_date.strftime("%A, %b %d") %>. Games will be played at <%=l.locations%>.</strong>
							</div>
							<div class="btn-group btn-group-justified">
							  <div class="btn-group">
									<button data-toggle="modal" data-target="#<%= l.id %>league" data-remote="<%= edit_league_path(l) %> #edit_league_modal" class="btn btn-default" >
										<span class="glyphicon glyphicon-edit"></span>
									</button>
									<div class="modal fade" id="<%= l.id %>league" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
									  <div class="modal-dialog">
									    <div class="modal-content">
									    </div>
									  </div>
									</div>
							  </div>
							  <div class="btn-group">
							    <button type="button" class="btn btn-default"><%= link_to 'Delete', l, method: :delete, data: {confirm: "Are you sure you want to delete #{l.name}"}%></button>
							  </div>
							</div>

							<br>
						
					<% end %>

				</div>		
					<button data-toggle="modal" data-target="#leagueModal" data-remote="<%= new_league_path %> #new_league_modal" class="btn btn-default btn-block">+ League</button>
				<br>
			</div>


			<div class="col-md-3">
				
				<%= render 'schedule' %>

			</div>


			<div class="col-md-3">
				
				<%= render 'standings' %>

			</div>


			<div class="col-md-3">
				
				<%= render 'results' %>

			</div>

		</div>
	</div>

<% else %>

	<div class="container-fluid">

		<div class="row">
		
			<div class="col-md-4">
				
				
					<span class="list-group-item">
						<p id="myTitle" name="scheduleTitle">( <%=@league.name%> )</p>
					</span>
					<div class="list-group-item">
							<strong class="list-group-item-text">This league starts <%= @league.start_date.strftime("%A, %b %d") %> and runs through <%= @league.end_date.strftime("%A, %b %d") %>. Games will be played at <%=@league.locations%>.</strong>
					</div>
					<%= link_to 'Back to Leagues', leagues_path, class: 'btn btn-default btn-block' %>
				
				<br>
			
				<%= render 'schedule' %>

			</div>


			<div class="col-md-4">
				
				<%= render 'standings' %>

			</div>


			<div class="col-md-4">
				
				<%= render 'results' %>

			</div>

		</div>
	</div>

<% end %>

<br>
<br>







<div class="modal fade" id="leagueModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
    </div>
  </div>
</div>
<div class="modal fade" id="userModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
    </div>
  </div>
</div>
<div class="modal fade" id="signinModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
    </div>
  </div>
</div>
<div class="modal fade" id="teamModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
    </div>
  </div>
</div>
<div class="modal fade" id="dayModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
    </div>
  </div>
</div>












